def fac(n):

    if(n > 1 ):
        return n*fac(n-1)
    
    else:
        return n
    
n = int(input("Ingresa el número de índice para calcular el Factorial "))
resultado = fac(n)
print(f"El factorialde {n} es: {resultado}")
